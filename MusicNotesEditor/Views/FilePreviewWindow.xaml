<Window x:Class="MusicNotesEditor.Views.FilePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Preview - {Binding FileItem.FileName}"
        Height="700" Width="800"
        WindowStartupLocation="CenterOwner"
        MinWidth="600" MinHeight="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{Binding FileItem.FileName}" 
                   FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- High Resolution Preview -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" 
                Background="White" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="{Binding HighResolutionImage}" 
                   MaxWidth="750" 
                   MaxHeight="550"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
        </Border>

        <!-- File Information -->
        <StackPanel Grid.Row="2" Margin="0,10,0,5">
            <TextBlock Text="{Binding FileInfo}" FontSize="12" Foreground="Gray"/>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="3" Content="Close" 
                Width="80" Height="30" 
                HorizontalAlignment="Center"
                Click="CloseButton_Click"/>
    </Grid>
</Window>